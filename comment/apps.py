from django.apps import AppConfig

# There are some bugs in this function, in the progress2 need the fix.
# Comment stare only 9 and 10, can not comment other points.
class CommentConfig(AppConfig):
    name = 'comment'
